var urc_atom_examples_config = (function () {
    'use strict';

    // ./ = ./
    const configBaseUrl = './';
    const config = {
        some: 'index',
        configBaseUrl,
        assetsUrl: `${configBaseUrl}i`,
        id: 'urc_atom_examples'
    };

    return config;

}());
